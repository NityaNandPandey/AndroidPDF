<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">


    <com.pdftron.pdf.widget.recyclerview.SimpleRecyclerView
        android:id="@+id/colormode_preset_recycler"
        android:layout_width="@dimen/preset_view_width"
        android:layout_height="fill_parent"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/linearLayout3"/>

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:paddingRight="14dp"
        android:paddingEnd="14dp"
        android:paddingBottom="14dp"
        android:id="@+id/linearLayout3"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true">

        <Button
            android:layout_width="wrap_content"
            android:layout_height="40sp"
            android:text="@string/cancel"
            android:id="@+id/colormode_preset_cancelbtn"
            android:textSize="12sp"
            android:background="?attr/selectableItemBackground"
            android:textColor="?attr/colorAccent" />

        <Button
            android:layout_width="48dp"
            android:layout_height="40sp"
            android:text="@string/ok"
            android:id="@+id/colormode_preset_okbtn"
            android:textSize="12sp"
            android:background="?attr/selectableItemBackground"
            android:textColor="?attr/colorAccent" />
    </LinearLayout>

</RelativeLayout>
